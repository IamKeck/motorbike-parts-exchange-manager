{
  "name": "motorbike-parts-exchange-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:elm": "elm make src/Main.elm --output dist/main.js",
    "build:sw": "ts-node src/sw.ts",
    "build": "npm run build:elm && npm run build:sw"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/workbox-build": "^5.0.0",
    "elm": "*",
    "elm-format": "^0.8.5",
    "elm-test": "*",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4",
    "workbox-build": "^6.1.5"
  }
}
